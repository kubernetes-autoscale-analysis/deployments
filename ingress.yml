apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wasm-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wasm-service
            port:
              number: 80
